
```markdown
# Exercise 5: PR Summarizer with GitHub Copilot SDK

## 🎯 Objective
Use **GitHub Copilot SDK** to create an AI agent that automatically analyzes and summarizes pull requests generated by the self-healing workflow.

---

## 📋 Prerequisites

- Completed [Exercise 4](EXERCISE_4_SELF_HEALING.md)
- Self-healing PR created (e.g., PR #50)
- Python 3.11+
- GitHub Personal Access Token


---

## 📚 What is Copilot SDK?

GitHub Copilot SDK allows you to build custom AI agents that:
- Understand code context deeply
- Analyze changes intelligently
- Generate human-like summaries
- Integrate with GitHub workflows

**Reference:** [Building Agents with GitHub Copilot SDK](https://techcommunity.microsoft.com/blog/azuredevcommunityblog/building-agents-with-github-copilot-sdk-a-practical-guide-to-automated-tech-upda/4488948)

---

## 🏗️ Part 1: Setup Copilot SDK Environment

### Step 1.1: Create Agent Structure

**Create directory structure:**
```powershell
# Navigate to project root
cd C:\Users\kavyashrias\Downloads\Copilot_SDK

# Create agent directory
mkdir agents\pr_summarizer
cd agents\pr_summarizer
```

### Step 1.2: Install Copilot SDK

**Create `requirements-agent.txt`:**
```txt
# GitHub Copilot SDK
copilot-sdk>=0.1.0

# GitHub Integration
PyGithub>=2.1.1

# Environment & Config
python-dotenv>=1.0.0
pydantic-settings>=2.0.0

# OpenAI (Copilot backend)
openai>=1.0.0

# Utilities
rich>=13.0.0
```

**Install dependencies:**
```powershell
pip install -r agents\pr_summarizer\.requirements-agent.txt
```
#use complete path to install

### Step 1.3: Configure Environment

**Create `.env` file:**
```env
# GitHub Access
GITHUB_TOKEN=ghp_your_token_here
GITHUB_REPOSITORY=CanarysPlayground/Copilot_SDK

# OpenAI/Copilot API
OPENAI_API_KEY=your_openai_key_here

# Agent Settings
COPILOT_MODEL=gpt-4
```

---

## 🤖 Part 2: Build the Copilot Agent

### Step 2.1: Create Configuration

**File: `agents/pr_summarizer/config.py`**

**Prompt to Copilot:**
```
Create a Pydantic settings class to load GitHub and OpenAI configuration from environment variables
```
### Step 2.2: Create Copilot-Powered Analyzer

**File: `agents/pr_summarizer/copilot_analyzer.py`**

**Prompt to Copilot:**
```
Create a class that uses Copilot SDK to analyze PR changes with intelligent context understanding. Include methods to generate summaries with code-aware insights.
```



### Step 2.3: Create GitHub Integration

**File: `agents/pr_summarizer/github_integration.py`**

**Prompt to Copilot:**
```
Create a GitHub client using PyGithub to fetch PR details, get file changes, retrieve diffs, and post AI-generated comments
```
### Note:
Yes, if you want the agent to run non-interactively (local script or GitHub Actions) and post comments automatically, you need a GitHub API client in code (PyGithub or direct REST). The GitHub MCP/CLI is great for manual use, but it won’t be available inside your Python agent unless you shell out and handle auth/output parsing.


### Step 2.4: Create Main Agent

**File: `agents/pr_summarizer/agent.py`**

**Prompt to Copilot:**
```
Create the main agent entry point that orchestrates PR fetching, Copilot analysis, and comment posting. Include CLI argument parsing.
```

---

## 🔄 Part 3: Automate with GitHub Actions

### Step 3.1: Create Workflow

**File: `.github/workflows/copilot-pr-summarizer.yml`**

**Prompt to Copilot:**
```
Create a GitHub Actions workflow that triggers on PR open/update and runs the Copilot PR summarizer agent
```

---

## 🧪 Part 4: Test the Agent

### Step 4.1: Test Locally

**Run the agent:**
```powershell
# Activate virtual environment
.\.venv\Scripts\Activate.ps1

# Set environment variables
$env:GITHUB_TOKEN = "your_token"

$env:GITHUB_REPOSITORY = "CanarysPlayground/Copilot_SDK"

# Test with self-healing PR
python -m agents.pr_summarizer.agent --pr <num>
```

**Expected output:**
```
🤖 Copilot PR Analyzer
Analyzing PR #50...

📥 Fetching PR data...
✓ PR: 🔧 Self-Healing: Auto-fix code formatting and dependencies
Files changed: 1

🧠 Analyzing with Copilot SDK...
💬 Posting summary comment...

✅ Successfully analyzed PR #50
4
Generated Summary:
[Copilot analysis appears here]
```

### Step 4.2: Test with GitHub Actions

**Commit and push:**
```powershell
# Add agent files
git add agents/
git add .github/workflows/copilot-pr-summarizer.yml
git add agents/pr_summarizer/.requirements-agent.txt

# Commit
git commit -m "feat: Add Copilot SDK PR Summarizer"

# Push
git push origin workshopBranch
```

**Create a test PR:**
```powershell
# Create test branch
git checkout -b test/copilot-summarizer

# Make a change
Add-Content -Path "README.md" -Value "`n## Copilot Test"

# Push and create PR
git add README.md
git commit -m "test: Trigger Copilot summarizer"
git push origin test/copilot-summarizer

gh pr create --title "Test: Copilot PR Summarizer" --base workshopBranch
```

---

## 📊 Part 5: Verify Results

### Step 5.1: Check PR Comment

**View the PR:**
```powershell
gh pr view <pr-number> --web
```

**Expected comment:**
```markdown
## 🤖 Copilot-Powered PR Analysis

### 🎯 Summary
This PR adds...

### 🔍 Code Analysis
Key changes include...

### ⚠️ Potential Issues
- Consider...

### ✅ Quality Check
- Code follows best practices
- Proper error handling

### 🧪 Testing
- Test coverage: Good
- Recommend testing...

### 📊 Recommendation
✅ **Approve** - Safe to merge

---
*Generated by GitHub Copilot SDK • Automated Code Review*
```

---

## 🎯 What Makes This "Copilot SDK"?

### Key Differences from Regular AI:

1. **Code Context Awareness**
   - Understands programming languages
   - Recognizes patterns and idioms
   - Knows best practices

2. **Repository Understanding**
   - Analyzes file relationships
   - Understands project structure
   - Considers dependency impacts

3. **Intelligent Summarization**
   - Focuses on developer concerns
   - Highlights breaking changes
   - Provides actionable feedback

4. **Integration Native**
   - Built for GitHub workflows
   - Seamless PR commenting
   - Action-triggered automation

---

## ✅ Exercise Completion Checklist

- [ ] Installed Copilot SDK dependencies
- [ ] Created agent configuration
- [ ] Built Copilot analyzer
- [ ] Integrated with GitHub API
- [ ] Created main agent entry point
- [ ] Set up GitHub Actions workflow
- [ ] Tested locally with self-healing PR
- [ ] Deployed to GitHub Actions
- [ ] Verified PR comment generated
- [ ] Understood Copilot SDK benefits

---

## 🎓 Learning Outcomes

You now know how to:

- ✅ Use GitHub Copilot SDK for code analysis
- ✅ Build AI agents with code understanding
- ✅ Integrate AI into GitHub workflows
- ✅ Generate intelligent PR summaries
- ✅ Automate code reviews with AI
- ✅ Deploy agents on GitHub Actions

---

## 🚀 Next Steps

- Enhance prompts for better insights
- Add code quality metrics
- Implement risk scoring
- Create multi-agent workflows
- Build custom Copilot extensions

---

**Powered by GitHub Copilot SDK 🤖✨**
```

This streamlined version focuses specifically on using Copilot SDK for PR summarization with clear, actionable steps!This streamlined version focuses specifically on using Copilot SDK for PR summarization with clear, actionable steps!

reference this content and give the steps to folow here for this repo